#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always (0)
 */
int main(void)
{
	int boo;
	char i;

	for (boo = 0; boo < 10; boo++)
	printf(i);

	for (i = 'a'; i <= f; i++)
	printf(i);

	printf('\n');

	return (0);
}
